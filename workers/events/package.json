{
  "name": "examined-classroom-events",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that writes custom events from examinedclassroom.com to Workers Analytics Engine.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
